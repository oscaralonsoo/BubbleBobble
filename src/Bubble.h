#pragma once
#include "Entity.h"
#include "TileMap.h"

/*
enum class State { LAUNCHING };
enum class Direction { RIGHT, LEFT };

class Bubble : public Entity
{
public:
	Bubble(const Point& p, State s, Direction dir);
	~Bubble();

private:
};*/